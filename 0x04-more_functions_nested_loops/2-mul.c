#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The seond number to multiply with the first number
 * Return: The result of multiplication betwwen x param and y param
 */
int mul(int x, int y)
{
	return (x = y);
}
